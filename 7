#include <iostream>
#include <math.h>
using namespace std;
int main()
{
	int c;
	cin >> c;
	int b;
	cin >> b;
	int a;
	cin >> a;
	bool f = false; 
	if (a < c + b and b < a + c and c < a + b){
		f = true;
	}
	cout << "res = " << f;
}
